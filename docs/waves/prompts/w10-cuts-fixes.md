You are a senior full-stack engineer. Work autonomously until fully done.

FIRST read `docs/waves/PHASE2-RULES.md` (mandatory rules) and `docs/waves/API-CONTRACT.md`, then `docs/waves/reports/W1A-AUDIT.md` (items A-020, A-025, A-030), `docs/waves/reports/W5-FINAL.md` ("Remaining risks"), `src/lib/security/rateLimit.ts` + `src/lib/security/rateLimit/`, `src/components/layout/headerData.ts`, `src/app/sitemap.ts`, and `src/app/admin/**`.
Wave id: W10. Branch `wave/w10-cuts-fixes`. Your dev port: **3303**. Report: `docs/waves/reports/W10-CUTS-FIXES.md`.

OWNER DECISION (2026-09-24): "Temporarily CLOSE the SpinWheel, the jobs page (/ish), testimonials (/testimoniyalar), and every feature that is not really built. Fix all known errors." "Close" means hidden and unreachable but reversible: keep the code, so the owner can reopen later with a one-line change. `/ekspertlar` stays open. Do not touch the home page sections (another agent is redesigning them) except to remove a closed feature's link.

BUILD:
1. **Closed-features registry** `src/lib/features/closed.ts`: a typed const map such as `{ jobs: { closed: true, routes: ["/ish"] }, testimonials: {...}, spinWheel: {...}, ... }` plus helpers `isClosed(key)` and `closedRoutePrefixes()`. One place to reopen anything.
   - Closed routes return a real 404 (`notFound()`) from the page/layout, including `/ish/[slug]`. Their API routes return 404 in the v1 error envelope (`fail(new ServiceError("NOT_FOUND", ...))`) or the legacy shape for legacy routes.
   - Remove them from header/footer navigation, mobile menu, sitemap, robots, site search index, internal links and CTAs, JSON-LD, and the admin sidebar. `grep` the whole `src` for every href.
   - SpinWheel: make sure nothing renders or imports it (keep the file). Remove any related promo copy.
   - Testimonial content: no testimonial or rating block renders anywhere on public pages (course pages, pricing, etc.).
2. **Unbuilt features audit.** Search the public site AND the admin panel for anything that looks available but does not work: "tez orada", "coming soon", disabled or no-op buttons, tabs or pages with mock/static data pretending to be live, env-gated features whose env is never set, dead admin settings. For each one decide: **works** → keep; **not built** → close via the registry (hide its entry points). Put a table of every item and your decision in the report. Do not delete working features.
3. **Shared rate limiting without Upstash.** Today `checkRateLimit` falls back to per-instance memory when Upstash env is missing, which is useless on Vercel serverless. Add a Postgres-backed backend behind the SAME `checkRateLimit` signature: new table `rate_limit_buckets` (key text PK = prefix:hashed-id, window_start timestamptz, count int) in `src/db/schema/security.ts`, using an atomic upsert (`INSERT ... ON CONFLICT DO UPDATE SET count = CASE WHEN window expired THEN 1 ELSE count+1 END ... RETURNING`). Order: Upstash when env is set → Postgres → memory. If the DB is down, fail OPEN for reads and low-risk writes, keep the existing behaviour for auth endpoints, and never throw a 500. Hash identifiers (IP) before storing. Add a cleanup of expired rows to the existing cron route. Write tests for the window, expiry, concurrency semantics and fallback order.
4. **Error-message leak check.** `errorResponse` (`src/lib/http/errors.ts`) returns `error.message` for unknown 500 errors. For status ≥ 500 that are not ServiceError, return the generic Uzbek recovery message instead and log the real error server-side. Update tests.
5. **Secrets hygiene audit (read-only on secrets).** `git grep` for committed tokens/keys/passwords (patterns: `bot[0-9]+:`, `sk-`, `AKIA`, `postgres://`, `password=`, long base64). Also check that no admin settings endpoint returns stored credentials to the browser. Fix any leak in code; NEVER print secret values in the report. Give the owner a list of which credentials to rotate (names only).
6. Keep the job-application code paths compiling and tested but unreachable (A-030 is moot while closed; note it in the report).

BUDGET: no public page may grow in First Load JS. Removing links and islands should shrink it — record before/after for `/`, `/kurs/[slug]`, `/portfolio`.
GATE: see PHASE2-RULES "Definition of done", plus a test that every closed route returns 404 and is absent from the sitemap.
