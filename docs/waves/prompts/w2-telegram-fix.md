You are a senior full-stack engineer. Work autonomously until fully done. You are continuing wave W2 (Telegram deep-link signup/login) on branch wave/w2-telegram.
FIRST read: docs/waves/PLAN.md §4 "W2 design", docs/waves/prompts/w2-telegram.md (original spec), docs/waves/reports/W2-TELEGRAM.md, docs/CODER_AGENT_RULES.md, then `git show HEAD`.

The orchestrator code-reviewed commit 0458ca2 and REJECTED it. The flow cannot work in production. Fix ALL of these (root causes, not patches):

1. BROKEN POLLING: TelegramAuthFlow derives the poll `id` from the deep link's `start` payload — that is the secret TOKEN, not the request UUID. /status validates a UUID → always 400 → login never completes, and the secret leaks into URLs/logs. Fix: store the `id` returned by /start in state and poll with it. The token must never appear in any site URL.
2. FAKE QR: `MiniQr` draws a random hash pattern that no phone can scan — dishonest UI. Replace with a REAL QR encoder: either a tiny dependency (e.g. `qrcode-generator` ~ 5 kB gz, or `uqr`) rendered to inline SVG, or a correct self-written encoder with tests. Show QR only on desktop (pointer: fine, ≥ 640px), and make it scannable (quiet zone, error correction M, dark modules on white in both themes).
3. SERVERLESS STATE BUG: `pendingContacts` is an in-memory Map. On Vercel the /start message and the contact message can hit different instances → the contact step loses the token. Remove the Map. On `/start login_<token>`: bind the request row to the Telegram user (set `tg_user_id`, keep status pending, only if still pending/unexpired). On contact: find the newest pending, unexpired request WHERE tg_user_id = from.id and approve it. All state in the DB.
4. POLL LOOP BUG: the polling effect depends on `seconds`, so it tears down and restarts every second, stacking requests. Use a single loop driven by refs / an absolute deadline (expiresAt from /start); countdown is display-only. Backoff 2s→3s→5s, pause while `document.hidden`, resume on visibilitychange, stop on unmount/success/expiry, abort in-flight fetch with AbortController.
5. CONSUME ORDER: getTelegramLoginStatus marks the request consumed BEFORE creating the session; if session creation fails the user is stuck. Do consume + session creation atomically in one transaction.
6. BOT MESSAGES: replies that interpolate user names use `parse_mode: "HTML"` without escaping → broken/injectable formatting. Drop parse_mode where not needed or escape. After contact, remove the reply keyboard (`Markup.removeKeyboard()`) and send the inline "Saytga qaytish" button. Handle `CONFLICT` (Telegram already linked to another phone) with a clear Uzbek message, not "link expired".
7. COOKIE: `Secure=${bool}` emits `Secure=false`, which browsers still treat as Secure. Use `response.cookies.set(...)` with proper options for both cookies.
8. ANTIFRAGILE: /status when DB is down → 503 JSON; the UI then shows "Telegram orqali kirish vaqtincha ishlamayapti — telefon raqami orqali davom eting" and focuses the phone input. Non-JSON responses must not crash the client (guard `response.json()`).
9. AUTH MODAL: confirm title/copy "Kirish yoki ro'yxatdan o'tish", Telegram flow first, phone/OTP as alternative, the old widget only behind NEXT_PUBLIC_TELEGRAM_WIDGET=1. On success: refresh auth state and close the modal after ~1.2 s (and honour a pending redirect if AuthContext has one).
10. TESTS (mandatory, vitest, with repository/db mocks): service — start creates hashed nonce; start with DB down → 503; bind on /start; already-linked approve; contact → new user created + audit log; contact → existing phone linked; contact → CONFLICT; expired; replay/consumed; wrong initiator cookie → unknown; status approved → exactly one session and single use under two concurrent calls. Routes — start sets httpOnly cookie; status rejects bad id (400), no cookie (unknown), approved sets session cookie and clears tg_login cookie. Bot handlers — /start login_ binds and prompts contact; contact ownership mismatch rejected. QR — encode/decode round-trip or snapshot against a known-good matrix.
Also update docs/waves/reports/W2-TELEGRAM.md with an "Orchestrator review fixes" section (each item → how fixed) and the owner's manual test steps.
Dev server: port 3203; stop ONLY with `kill $(lsof -t -i:3203)` — NEVER `pkill -f`. No Playwright.
GATE: `npx tsc --noEmit`, `npx vitest run` (test count must increase), `npm run build` — all green.
Commit: `git add -A && git commit -m "fix(auth): Telegram flow — real QR, DB-backed bot state, correct polling, tests"`. Do not push.
