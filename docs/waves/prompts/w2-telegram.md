You are a senior full-stack engineer specialising in auth systems and Telegram bots. Work autonomously until fully done.

FIRST read: docs/waves/PLAN.md (esp. §4 "W2 design"), docs/CODER_AGENT_RULES.md, docs/waves/reports/W1A-AUDIT.md (auth + Telegram findings), then src/features/auth/**, src/app/api/auth/**, src/lib/telegram/** (bot.ts, handlers/start.ts, handlers/contact.ts, linkToken.ts, linkAccount.ts, handoff.ts), src/db/schema/users.ts, src/context/AuthContext*.
If docs/waves/reports/W2-TELEGRAM.md exists, a previous run was interrupted: read it + git state and CONTINUE.

PROBLEM: Telegram only logs in existing users. A new user gets `phone_link_required` (422) and an error message. The Login Widget also depends on @BotFather /setdomain. Owner requirement: "not registered → register; registered → log in", seamless, one button.

BUILD (bot deep-link auth, see PLAN §4):
1. DB: new table `telegram_login_requests` (id uuid, nonce hash, status pending|approved|consumed|expired, user_id nullable FK, tg_user_id, created_at, expires_at, approved_at, consumed_at, ip, user_agent). Drizzle schema + migration `drizzle/0004_*.sql` (generate with `npm run db:generate`; DB is DOWN so do not run migrate — just make the SQL correct and idempotent-safe). Repository in src/features/auth/server.
2. `POST /api/auth/telegram/start` → rate-limited, creates request (TTL 5 min), returns `{ id, deepLink: "https://t.me/<BRAND.telegramBot>?start=login_<token>" }`. Token = opaque random (≥128 bit) whose hash is stored; `start` payload max 64 chars [A-Za-z0-9_-].
3. Bot `/start login_<token>`: already-linked Telegram user → approve with that user. Otherwise reply (Uzbek, friendly, uses BRAND.name) with a one-time reply keyboard "📱 Raqamni ulashish" (request_contact). On contact: reuse existing ownership check (contact.user_id === from.id), normalize phone (+998…), find user by phone → link tg id; none → CREATE student user (phone, full name from Telegram, tg id, username) inside a transaction; approve request. Remove keyboard, reply "✅ Tayyor! Saytga qayting." with an inline URL button back to the site. Expired/consumed/unknown token → clear message + how to retry. Idempotent under Telegram retries.
4. `GET /api/auth/telegram/status?id=<id>` → pending | approved (then atomically mark consumed + create session + set the same httpOnly session cookie as other logins; single use) | expired. Rate-limited. The polling client must prove it is the initiator: bind the request to an httpOnly short-lived cookie set by /start (compare hash) so a leaked id cannot be used by someone else.
5. UI (src/features/auth/components): new `TelegramAuthFlow` as the PRIMARY Telegram option in AuthModal: button "Telegram orqali davom etish" → calls start → opens deep link (window.open; on mobile location.href fallback) → shows a live waiting state ("Telegram'da botni oching va Start bosing…", QR code of the deep link for desktop users — render QR as inline SVG with a tiny dependency-free encoder or a < 5 kB gz lib; justify), polling every 2 s with backoff, stops when tab hidden and resumes, 5-min countdown, retry button, success "Xush kelibsiz, <ism>!" then refresh auth state (no hard reload if AuthContext supports it). Modal title/copy: "Kirish yoki ro'yxatdan o'tish". Keep the phone/OTP form as the alternative. The old Login Widget: keep only if NEXT_PUBLIC_TELEGRAM_WIDGET=1, and on its 422 fall back to TelegramAuthFlow instead of an error.
6. Antifragile: DB down → start returns 503 with Uzbek message and the UI offers the phone/OTP path; bot down → timeout message; all errors mapped to Uzbek UI text, never raw codes.
7. Security: no open redirect, no user enumeration, request single-use, constant-time comparisons, audit log entry on signup via Telegram, rate limits on start/status/bot handler.
8. Tests (vitest): service unit tests for all branches (new user signup, existing by phone, already linked, expired, consumed, replay, wrong initiator cookie, contact ownership mismatch), route tests for start/status.
Brand strings: `import { BRAND } from "@/config/brand"`.
SCOPE: auth + telegram + needed schema/migration + AuthModal UI. Do not touch animations or unrelated pages.
Dev server if needed: port 3203; stop ONLY with `kill $(lsof -t -i:3203)` — NEVER `pkill -f`. Do not run Playwright.
GATE: `npx tsc --noEmit`, `npx vitest run`, `npm run build` all green.
Report docs/waves/reports/W2-TELEGRAM.md (flow diagram, files, env vars, manual test steps for the owner once DB is back). Write incrementally.
Commit: `git add -A && git commit -m "feat(auth): Telegram deep-link signup + login"`. Do not push.
