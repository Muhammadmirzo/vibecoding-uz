You are a senior full-stack engineer + product designer. Work autonomously until fully done.

FIRST read: docs/waves/PLAN.md §5, docs/CODER_AGENT_RULES.md, src/db/schema/operations.ts (portfolios table), src/features/portfolio/**, src/app/portfolio/**, src/app/admin/portfolio/**, src/app/api/**/portfolio*, src/components/sections/home/Projects.tsx, docs/waves/reports/W4B-FIXES.md.
If docs/waves/reports/W6B-PORTFOLIO.md exists, a previous run was interrupted: read it + git state and CONTINUE.

OWNER REQUEST: "Some portfolio projects are not mine, they were just inserted. When I add a new project from the admin panel it must be placed in the best way across the whole site without breaking anything — the most important ones shown first — and visitors can choose to see all the others too."

BUILD:
1. Data model (Drizzle + migration `npm run db:generate` → next number after 0005; the DB is LIVE now — migration must be additive and safe): portfolios gets `ownership` enum-like text ('owner' | 'student' | 'client' | 'demo'), `featuredRank` int nullable (1 = first on home), `sortOrder` int, `status` ('published' | 'draft' | 'hidden'), `coverUrl`, `liveUrl`, `repoUrl`, `techStack` text[], `highlights` text[] (short proof points), `publishedAt`. Keep existing columns working.
2. Single source: public pages read from the DB via a cached repository (`unstable_cache` + tag "portfolio", revalidate 300) with a FAIL-SAFE fallback to the current static `portfolioData` only if the DB errors — but the static fallback must contain ONLY projects marked verified in code comments (Clash Nexus is the only verified owner project today); move every other static entry to `ownership: 'demo'`, status 'hidden' so nothing unverified is shown by default. The owner will confirm which projects are really theirs.
3. Ranking rules (document in the report): home shows up to 3 items: status published, ordered by featuredRank asc, then sortOrder, then publishedAt desc; owner projects before student/client. /portfolio shows "Asosiy loyihalar" (featured) first, then "Barcha loyihalar" with filter chips (Barchasi / Mening loyihalarim / Talabalar / Mijozlar) and a "Ko'proq ko'rsatish" load-more (no layout jump). Empty states are designed, never blank.
4. Admin (`/admin/portfolio`): list with drag-free reorder (up/down buttons + rank input, accessible), toggle featured (max 3 featured, enforced server-side with a clear message), ownership select, status select, create/edit form with Zod validation, image URL preview, live URL check hint. Every write: admin auth inside the handler, CSRF (requireAdmin with request), rate limit, audit log, `revalidateTag("portfolio")` so the home page updates immediately.
5. Seed/fix script `scripts/portfolio/mark-unverified.ts` (dry-run by default, `--apply` to write) that sets every existing DB portfolio row except Clash Nexus to ownership 'demo' + status 'hidden'. Do NOT run with --apply — the orchestrator decides after the owner confirms.
6. Tests: repository ranking/order, fallback on DB error, featured-limit enforcement, route auth (401/403), validation (400), revalidation called.
Every source file ≤ 250 lines (owner rule). Uzbek UI copy, theme tokens only.
PARALLEL SAFETY: another agent (W6A design) edits src/components/sections/home/** and globals.css — touch Projects.tsx only to switch its data source (keep its markup/classes otherwise), do not edit globals.css. Heavy commands ONLY via the lock: `scripts/waves/locked.sh npm run build`. No Playwright, no Lighthouse. Dev server only if needed on port 3302; stop ONLY with `ss -ltnp | grep ':3302 ' | grep -o 'pid=[0-9]*' | cut -d= -f2 | xargs -r kill` — NEVER `pkill -f`. Do NOT run `db:migrate` (orchestrator applies migrations).
GATE: `npx tsc --noEmit`, `npx vitest run`, `scripts/waves/locked.sh npm run build`.
HANDOFF: docs/waves/reports/W6B-PORTFOLIO.md (written incrementally): schema, ranking rules, admin guide in Uzbek for the owner, migration file name, what the orchestrator must run. Commit on your branch. Do not push.
