import { CheckCircle2, Users } from "lucide-react";
import type { ReferralLead } from "./referralTypes";

export function ReferralHistoryTable({ referralList }: { referralList: ReferralLead[] }) {
  if (referralList.length === 0) return <section className="rounded-2xl border border-dashed border-border-strong bg-bg-elevated p-8 text-center"><Users className="mx-auto h-8 w-8 text-ink-subtle" aria-hidden="true" /><h2 className="mt-3 font-display text-lg font-semibold text-ink">Takliflar ro&apos;yxati bo&apos;sh</h2><p className="mx-auto mt-2 max-w-lg text-base leading-relaxed text-ink-muted">Havolangiz orqali kelgan foydalanuvchilar tizimda qayd etilganda ular ro&apos;yxati shu yerda ko&apos;rinadi.</p></section>;
  return <section className="overflow-hidden rounded-2xl border border-border bg-bg-elevated p-6 md:p-8"><div className="flex items-center gap-2 border-b border-border pb-4"><Users className="h-5 w-5 text-accent" aria-hidden="true" /><h2 className="font-display text-lg font-semibold text-ink">Taklif qilingan do&apos;stlar</h2></div><div className="mt-4 overflow-x-auto"><table className="w-full min-w-[600px] text-left text-sm"><thead><tr className="border-b border-border text-ink-subtle"><th className="pb-3 pr-4 font-semibold">Do&apos;stingiz</th><th className="px-4 pb-3 font-semibold">Sana</th><th className="px-4 pb-3 font-semibold">Holat</th><th className="pl-4 pb-3 text-right font-semibold">Bonus</th></tr></thead><tbody className="divide-y divide-border">{referralList.map((ref) => <tr key={ref.id}><th scope="row" className="py-4 pr-4 font-semibold text-ink">{ref.name}</th><td className="px-4 py-4 text-ink-muted">{ref.date}</td><td className="px-4 py-4">{ref.status === "paid" ? <span className="inline-flex items-center gap-1 rounded-full bg-success-soft px-2.5 py-1 text-xs font-semibold text-success"><CheckCircle2 className="h-3 w-3" />To&apos;lov qilindi</span> : <span className="text-ink-muted">Ro&apos;yxatdan o&apos;tdi</span>}</td><td className="pl-4 py-4 text-right font-mono font-semibold text-ink">{ref.bonusAmount}</td></tr>)}</tbody></table></div></section>;
}
