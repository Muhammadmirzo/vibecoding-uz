import { Layers } from "lucide-react";
import type { AnalyticsData } from "@/features/crm/types";
export function SourceBreakdown({ sources }: { sources: AnalyticsData["sources"] }) { const labels: Record<string, string> = { quiz: "Diagnostika Quiz", free_lesson: "Bepul dars", form: "Sayt formasi", telegram: "Telegram bot" }; return <div className="bg-bg-elevated border border-border rounded-xl p-6 shadow-sm space-y-4"><div className="border-b border-border pb-3"><h3 className="font-bold text-base text-ink flex items-center"><Layers className="w-4 h-4 mr-2 text-accent" />Leadlar Manbasi Taqsimoti (Lead Sources)</h3></div><div className="space-y-3">{sources.map((source) => <div key={source.name} className="flex items-center justify-between text-xs p-2.5 bg-bg-sunken rounded-lg border border-border"><span className="font-semibold text-ink capitalize">{labels[source.name] || source.name}</span><div className="flex items-center space-x-3"><span className="text-ink-muted">{source.count} ta lead</span><span className="font-bold text-accent px-2 py-0.5 rounded bg-bg-elevated border border-border">{source.percentage}</span></div></div>)}</div></div>; }
