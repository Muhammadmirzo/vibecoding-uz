import { ShoppingBag } from "lucide-react";
import type { AnalyticsData } from "@/features/crm/types";
import { formatMoney } from "./StatCards";
export function RevenueChart({ revenue, total }: { revenue: AnalyticsData["revenueByProvider"]; total: number }) { const item = (name: string, value: number, color: string) => <div className="space-y-1.5"><div className="flex justify-between text-xs font-medium text-ink"><span>{name}</span><span className="font-bold text-accent">{formatMoney(value)}</span></div><div className="w-full bg-bg-sunken h-2 rounded-full overflow-hidden border border-border"><div className={`${color} h-full`} style={{ width: `${total > 0 ? (value / total) * 100 : 0}%` }} /></div></div>; return <div className="bg-bg-elevated border border-border rounded-xl p-6 shadow-sm space-y-4"><div className="border-b border-border pb-3"><h3 className="font-bold text-base text-ink flex items-center"><ShoppingBag className="w-4 h-4 mr-2 text-accent" />To'lov Tizimlari Tushumi (Revenue by Provider)</h3></div><div className="space-y-4">{item("Payme", revenue.payme, "bg-accent")}{item("Click", revenue.click, "bg-brand")}{item("Manual Invoys (Bank / Naqd)", revenue.manual, "bg-success")}</div></div>; }
