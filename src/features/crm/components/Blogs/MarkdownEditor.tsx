"use client";
import type { EditorTab } from "./types";
export function MarkdownEditor({ tab, setTab, contentMd, setContentMd }: { tab: EditorTab; setTab: (tab: EditorTab) => void; contentMd: string; setContentMd: (value: string) => void; insert: (p: string, s?: string, d?: string) => void }) { return <div className="space-y-2"><div className="flex gap-1 border-b border-border pb-2">{(["edit", "split", "preview"] as EditorTab[]).map(t => <button type="button" key={t} onClick={() => setTab(t)} className={`px-3 py-1 text-xs rounded-md ${tab === t ? "bg-accent text-ink" : "text-ink-muted"}`}>{t === "edit" ? "Tahrirlash" : t === "split" ? "Yonma-yon" : "Oldindan ko'rish"}</button>)}</div>{tab === "preview" ? <div className="min-h-[240px] p-4 bg-bg-elevated border border-border rounded-lg whitespace-pre-wrap">{contentMd || <span className="italic text-ink-muted">Matn kiritilmagan...</span>}</div> : <textarea id="blog-content-textarea" rows={12} required value={contentMd} onChange={e => setContentMd(e.target.value)} className="w-full px-4 py-3 text-sm bg-bg-elevated border border-border rounded-lg font-mono" />}</div>; }
