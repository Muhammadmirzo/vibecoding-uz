"use client";
import { Info } from "lucide-react";
import { audienceLabels } from "./types";
import type { NotificationAudience, NotificationChannel } from "./types";
export function TelegramPreview({ channel, title, messageBody, targetAudience }: { channel: NotificationChannel; title: string; messageBody: string; targetAudience: NotificationAudience }) { const text = messageBody.replace(/{{fullName}}/g, "Alisher Zokirov").replace(/{{phone}}/g, "+998 90 123 45 67").replace(/{{courseTitle}}/g, "Vibe Coding Express").replace(/{{loginUrl}}/g, "https://vibecoding.uz/kabinet"); return <div className="lg:col-span-5 space-y-4"><div className="bg-bg-sunken border border-border rounded-xl p-5 space-y-4 sticky top-20"><h3 className="text-xs font-bold text-ink uppercase tracking-wider">{channel.toUpperCase()}</h3><div className="bg-ink text-ink rounded-2xl p-4 shadow-md text-xs"><div className="font-semibold">Mirzo Academy Bot</div><div className="bg-bg-sunken p-3 rounded-xl mt-2 whitespace-pre-wrap">{text}</div></div><div className="p-3 bg-bg-elevated border border-border rounded-lg text-xs text-ink-muted"><Info className="w-4 h-4 text-accent inline mr-1" />Auditoriya bo&apos;yicha taxminiy target: <strong>{audienceLabels[targetAudience]}</strong>. {title}</div></div></div>; }
