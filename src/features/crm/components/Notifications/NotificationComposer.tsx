"use client";
import { Layers, Loader2, Mail, MessageSquare, Send, Smartphone, Sparkles } from "lucide-react";
import type { Cohort, NotificationAudience, NotificationChannel } from "./types";
interface Props { cohorts: Cohort[]; sending: boolean; onSend: (status: "sent" | "draft") => void; title: string; setTitle: (v: string) => void; channel: NotificationChannel; setChannel: (v: NotificationChannel) => void; audience: NotificationAudience; setAudience: (v: NotificationAudience) => void; cohortId: string; setCohortId: (v: string) => void; body: string; setBody: (v: string) => void; }
export function NotificationComposer(p: Props) { const ins = (tag: string) => p.setBody(`${p.body} ${tag}`); return <div className="lg:col-span-7 bg-bg-sunken border border-border rounded-xl p-6 space-y-5"><h2 className="text-base font-bold text-ink border-b border-border pb-3 flex items-center gap-2"><Sparkles className="w-4 h-4 text-accent" />Yangi Xabarnoma Tuzish</h2><div><label className="block text-xs font-medium text-ink mb-1">Xabarnoma Nomi (Ichki foydalanish uchun) *</label><input type="text" placeholder="Masalan: Vibe Coding 3-modul darsi eslatmasi" value={p.title} onChange={e => p.setTitle(e.target.value)} className="w-full px-3.5 py-2 text-sm bg-bg-elevated border border-border rounded-lg focus:outline-none focus:ring-2 focus:ring-accent" /></div><div><label className="block text-xs font-medium text-ink mb-2">Yuborish Kanali *</label><div className="grid grid-cols-2 sm:grid-cols-4 gap-2">{([["telegram", MessageSquare], ["email", Mail], ["sms", Smartphone], ["all", Layers]] as const).map(([key, Icon]) => <button type="button" key={key} onClick={() => p.setChannel(key)} className={`flex items-center justify-center gap-2 px-3 py-2.5 rounded-lg border text-xs font-medium transition-colors ${p.channel === key ? "bg-accent text-ink border-accent shadow-sm" : "bg-bg-elevated text-ink border-border hover:bg-bg-sunken"}`}><Icon className="w-4 h-4" />{key === "all" ? "Barchasi" : key[0].toUpperCase() + key.slice(1)}</button>)}</div></div><div className="grid grid-cols-1 sm:grid-cols-2 gap-3"><select value={p.audience} onChange={e => p.setAudience(e.target.value as NotificationAudience)} className="w-full px-3.5 py-2 text-sm bg-bg-elevated border border-border rounded-lg focus:outline-none focus:ring-2 focus:ring-accent"><option value="all_users">Barcha foydalanuvchilar</option><option value="active_students">Faol talabalar</option><option value="cohort_students">Muayyan guruh talabalari</option><option value="leads_new">Yangi leadlar (Quiz topshirganlar)</option><option value="leads_consultation">Konsultatsiyadagilar</option></select>{p.audience === "cohort_students" ? <select value={p.cohortId} onChange={e => p.setCohortId(e.target.value)} className="w-full px-3.5 py-2 text-sm bg-bg-elevated border border-border rounded-lg"><option value="">Guruhni tanlang...</option>{p.cohorts.map(c => <option key={c.id} value={c.id}>{c.name}</option>)}</select> : null}</div><div><span className="block text-xs font-medium text-ink mb-1.5">Dinamik O'zgaruvchilar (Kliklang):</span><div className="flex flex-wrap gap-1.5">{["{{fullName}}", "{{phone}}", "{{courseTitle}}", "{{loginUrl}}"].map(tag => <button key={tag} type="button" onClick={() => ins(tag)} className="px-2.5 py-1 rounded-md text-xs bg-accent-soft text-accent border border-border hover:bg-accent hover:text-ink transition-colors font-mono">+ {tag}</button>)}</div></div><div><label className="block text-xs font-medium text-ink mb-1">Xabar Matni *</label><textarea rows={6} value={p.body} onChange={e => p.setBody(e.target.value)} className="w-full px-4 py-3 text-sm bg-bg-elevated border border-border rounded-lg font-sans" /><div className="flex justify-between items-center text-[11px] text-ink-muted mt-1"><span>Harlar soni: {p.body.length} ta</span>{p.channel === "sms" ? <span>SMS segmenti: {Math.ceil(p.body.length / 160) || 1} ta SMS</span> : null}</div></div><div className="flex items-center justify-end gap-3 pt-3 border-t border-border"><button type="button" disabled={p.sending} onClick={() => p.onSend("draft")} className="px-4 py-2 text-xs font-medium text-ink-muted hover:text-ink">Qoralama Saqlash</button><button type="button" disabled={p.sending} onClick={() => p.onSend("sent")} className="inline-flex items-center justify-center px-5 py-2.5 text-sm font-medium text-ink bg-accent rounded-lg disabled:opacity-50 gap-2">{p.sending ? <Loader2 className="w-4 h-4 animate-spin" /> : <Send className="w-4 h-4" />}Hozir Yuborish</button></div></div>; }
